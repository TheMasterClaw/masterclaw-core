# LLM Configuration
OPENAI_API_KEY=
ANTHROPIC_API_KEY=
DEFAULT_MODEL=gpt-4

# Memory
MEMORY_BACKEND=chroma
CHROMA_PERSIST_DIR=./data/chroma
EMBEDDING_MODEL=all-MiniLM-L6-v2

# API
PORT=8000
HOST=0.0.0.0
LOG_LEVEL=info

# =============================================================================
# Security Configuration (HSTS Headers - Production Only)
# =============================================================================
# HSTS (HTTP Strict Transport Security) headers are only sent in production
# environments (ENV=production or NODE_ENV=production) to prevent downgrade
# attacks and cookie hijacking.
#
# HSTS_MAX_AGE: How long browsers should enforce HTTPS (default: 31536000 = 1 year)
# HSTS_INCLUDE_SUBDOMAINS: Apply HSTS to all subdomains (default: true)
# HSTS_PRELOAD: Include in browser preload lists (default: false)
#
# WARNING: Only enable HSTS_PRELOAD if you are certain all subdomains support HTTPS
# HSTS_MAX_AGE=31536000
# HSTS_INCLUDE_SUBDOMAINS=true
# HSTS_PRELOAD=false
